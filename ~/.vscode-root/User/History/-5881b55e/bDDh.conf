events {}

http {

	# Mime типы (Директива массив)
	include mime.types;
	
	# Кодировка (Стандартная директива)
	charset utf8; 

	server {

		# Конфигурация для локального хоста (Стандартные директивы)
		listen 80;
		server_name localhost;

		# Корневая папка (Стандартная директива)
		root /var/www/myfirstapp;
	}
}