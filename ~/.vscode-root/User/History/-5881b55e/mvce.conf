events {}

http {

	# Mime типы
	include mime.types;
	
	# Кодировка
	charset utf8; 

	server {

		# Конфигурация для локального хоста
		listen 80;
		server_name localhost;

		# Корневая папка
		root /var/www/myfirstapp;
	}
}