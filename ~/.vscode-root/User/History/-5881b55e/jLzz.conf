events {}

http {

	# Пример директивы массива (может принимать набор файлов)
	include mime.types;
	
	# Пример стандартной директивы
	charset utf8; 

	# Пример стандартной директивы
	access_log /var/log/nginx/access.log;
	error_log /var/log/nginx/error.log debug;

	server {

		# Конфигурация для локального хоста
		listen 80;
		server_name localhost;

		# Корневая папка
		root /var/www/example; 

		# Этот блок не требуется, так как браузер
		# И так ищет файл index.html который находится в root
		# Будь бы название файла, или расположение другим
		# То нам бы пришлось его тут указать
		location / {
			index index.html;
		}

		# Поиск картинок
		location /pictures {
			# Переопределяем директиву логов
			access_log off;
			try_files $uri /$uri =404;
		}
	}
}